# 更新记录

## v0.1.4 — 2026-09-17

首个公开版本。写作体验剥离自 Hexo 博客的内置编辑器，做成独立的桌面 Markdown 编辑器。

### 它是什么

- **源码装饰**：`**粗体**` 的星号一直在，只是文字变粗。编辑器内部持有的永远是那个 `.md` 原文，
  没有「富文本 → 序列化回 Markdown」这一步，文件不会被悄悄改脏。图片行是唯一例外。
- **一窗一文**：没有目录树、没有标签页。`⌘N` 开新窗口，`⌘O` 换文件。
- **轻**：dmg 1.9MB / 应用 3.6MB；短文约 101MB 内存，十万字约 108MB（只多 7MB）；
  十万字文档滚动约 1.7ms/帧、单次输入 < 3ms。

### 保留自旧编辑器

标题装饰、行内装饰与引用左边线、图片行内渲染、左侧目录、右侧缩略图、专注模式（含可拖的渐隐曲线）、
段首两个半角空格转全角、自绘光标的呼吸闪烁、当前行与悬停行高亮、`⌘B/⌘I`、`⌘\` 抽屉。

与旧编辑器的**对照实验 23/23 项一致**（行宽 / 字号 / 行高 / 正文色 / 内边距 / 断词方式 / 标题 /
引用边线与悬挂缩进 / 标记色 / 代码底色与字体 / 链接色 / 纸色 / 字形度量 / 目录 / 缩略图比例 /
每段的换行行数），7 篇存量文章逐一比对。

### 与旧编辑器不同的地方

随博客管理层一并去掉：标题字段、标签、slug、草稿发布、`date` 打戳、文章列表、front-matter 解析。
新增：`⌘F` 查找、外部改动监听、设置面板。
口径改动：字数改为通用口径（中文按字、英文按词）；字体取自本机、可配置、不打包。
完整清单见 [`docs/acceptance/deviations.md`](docs/acceptance/deviations.md)。

### 界面

- **透明标题栏（macOS）**：顶栏就是红绿灯那一行（28px），横贯整个窗口都能拖（连左缘留白和缩略图顶端），
  双击放大；文件名居中在里面，对着文字列与卷首花饰的中线，名字前那颗点就是保存状态。
  Windows / Linux 用系统标题栏，文件名在正文上方单独一行。
- **卷首花饰**：正文最开头上方两根向中间收拢的发丝托着一颗空心菱形，提示这里是文本开头；
  不占行、跟着正文滚、专注模式下跟着第一行。
- 内容一屏放得下时页面不能滚、缩略图选框拖不动；放不下才在末尾留出「滚过末尾」的空白。
- **图标：等角投影的「囲炉裏」**。拆开看正好是「囲」这个字 —— 外面一圈黑是炉缘（口），
  里面四道凹槽是井桁框（井，也是 Markdown 的 `#`），正中那格再沉一层是火塘，亮着的是炭，
  升起来的是烟（取编辑器的 `--tok`，和你正在写的 `#` 同一档灰）。
  整块摆进一枚 macOS 圆角纸片（超椭圆 n=5，1024 画布 / 824 盘面）。
  > 标本身是七种纯色，没有渐变也没有半透明；所有几何由 `design/icon/hearth.gen.mjs`
  > 算出来，包括凹槽的遮挡关系和圆角 —— 改一个尺寸，透视和遮挡自己重排。
  > 设计文稿在 `design/icon/hearth.md`，矢量原件与各尺寸 PNG 在 `assets/logo/`。
  > 复现：`node design/icon/hearth.gen.mjs && node design/icon/appicon.gen.mjs`；
  > 过程产物统一落在 `design/icon/out/`，整个目录不进 git

### 平台

架构按三平台写（ADR-0001），CI 三平台都编译，但**只有 macOS 跑过验收**。
Windows / Linux 的产物属于「能编译能打包」，不算支持。

- Windows 安装包：在 Actions 页手动运行 Release，产物 `irori-Windows` 里是 `-setup.exe` 与 `.msi`；
  打 `v*` tag 则生成草稿 release。安装包没有签名，SmartScreen 会拦一下（「更多信息 → 仍要运行」）。
- 双击 / 命令行打开的路径在 Windows 上会被 `canonicalize` 加上 `\\?\` 前缀，启动时去掉，
  否则前端拼出的图片路径不合法。（未在真机 Windows 上验证）

### 验证

- 40 条单元断言（装饰规则 / 路径 / 字数 / 滑动曲线）
- **82 条行为用例 · 328 条断言**，headless Chromium 驱动真实应用（打包出来的 `dist/`）
- 与旧编辑器的对照实验，7 篇文章 23/23 一致
- 真机冒烟：在系统 WebView（WebKit）里跑打包产物，普通 16 ✓ / `--close` 17 ✓ / `--dialog` 17 ✓，0 ✗
  > 冒烟探针留在生产包里 —— 两个测试套件跑的都是生产产物（e2e 驱动 `dist/`、冒烟驱动打包出的 `.app`），
  > 「测的就是发出去的那一份」是刻意取舍，不能按 DEV 剔掉。
- 人工验收 12 条，见 [`docs/acceptance/behavior-checklist.md`](docs/acceptance/behavior-checklist.md)

### 开发中修掉的真机 bug

- **⌘S 保存卡死**：同步的 Tauri 命令跑在主线程，`blocking_save_file` 把它停住等对话框，
  而对话框又要靠主线程才能出现 —— 死锁。改为 async + 回调 + 阻塞线程池等待。
- **⌘W / 红灯弹了确认、点「关闭」窗口却不关**：Tauri 在关闭监听之后会自己 destroy 窗口，
  那一步缺 `core:window:allow-destroy` 权限而静默失败。补上权限，改为由应用自己销毁并暴露失败。
- **顶部拖不动窗口**：`data-tauri-drag-region` 要靠 drag.js 调 `start_dragging`，
  缺 `core:window:allow-start-dragging` 权限就什么都不发生（双击放大走的是默认权限，所以不容易发现）。
- **快捷键要先点进正文才生效**：窗口层补了一份处理，并在建窗后 `set_focus()`。
- **滚轮只在文字上有效**：整片书写区都成为滚动容器，缩略图与顶栏由窗口层兜底转发。
- **深色模式下顶部一条黑条**：透明标题栏 + 窗口外观锁浅色。
- **打开文件后滚过了开头**：CodeMirror 会把光标映射到插入文本末尾；现在打开即停在开头，
  外部改动触发的重载改用最小 diff，光标与滚动位置都留得住。
- **长跳转有概率没滚到底**：滚动途中文档总高度还在被测量修正，落点会偏近；加了落点复核。
- **光标双闪 / 一深一浅两条**：关掉 CodeMirror 自带的硬切闪烁，并清掉它那条黑色左边框。
- **专注模式渐隐盖住光标与抽屉**：渐隐层挂进编辑器内部，并让编辑器自成层叠上下文。
- **抽屉里点一下正文被拽回光标处**：WebKit 重新聚焦 contenteditable 会把光标滚进视野；
  现在抽屉开着不抢焦点，且聚焦一律保持滚动位置。
- **Windows 上编译不过**：`use tauri::Emitter` 只在 macOS 分支里用到，其它平台是未使用导入，
  `clippy -D warnings` 直接失败；改为只在 macOS 导入。
- **`./scripts/build.sh` 不带参数时直接报错退出**：macOS 自带的 bash 3.2 在 `set -u` 下把空的 `"$@"` 当成未定义变量。
