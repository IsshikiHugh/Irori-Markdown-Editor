# 更新记录

## v0.1.9 — 2026-09-19

### 修复

- **更新重启时不丢内容**：窗口保存完毕后即不再接受输入，直到重启；如果重启被取消，窗口恢复编辑。此前在等待另一个窗口保存时，回到已准备好的窗口继续打字，这些内容会在重启时丢失。
- **更新重启不再卡住**：重启前若有窗口恰好被关闭或尚未加载完，不再一直等待它（此前最长会停在「正在重启…」10 分钟）；两个窗口同时点「更新」也不会互相干扰。
- **外部改动时不覆盖对方的版本**：文件在外部被改动、正在询问保留哪个版本时，自动保存与 `⌘S` 都不会写入，关闭窗口或更新重启会等你做出选择。
- **带括号的链接**：网址中含括号的链接（如维基百科的 `…/Foo_(bar)`）不再被截断，`⌘`+点击打开的也是完整网址。

### 更新

- 除了启动时，Irori 开着的时候也会每天检查一次新版本，长时间不退出也能收到提示。

### 安装

- 已安装 v0.1.7 或更新版本的，打开 Irori 后会收到更新提示，点「更新」即可。
- **macOS**：`Irori_0.1.9_aarch64.dmg`（Apple Silicon）、`Irori_0.1.9_x64.dmg`（Intel）
- **Windows**：`Irori_0.1.9_x64-setup.exe` 或 `Irori_0.1.9_x64_en-US.msi`，任选其一
- **Linux**：`.deb`、`.rpm` 与 `.AppImage`

安装包尚未签名。macOS 若提示应用「已损坏」，可在终端执行
`xattr -dr com.apple.quarantine /Applications/Irori.app` 后再打开；
Windows 若出现 SmartScreen 提示，选择「更多信息 → 仍要运行」。

## v0.1.8 — 2026-09-19

### 书写

- **表格**：光标不在表格里时，Markdown 表格显示为真正的表格，按分隔行左对齐、居中或右对齐；单元格里的粗体、代码和链接照常显示，标记隐去。点任一单元格，表格变回源码，光标落在所点的那一格；用 ↑ ↓ 也能从上下两行走进表格，竖线与分隔行以浅色显示。缩略图与导出的 PDF 中同样显示为表格。
- **链接**：光标没有碰到链接时，只显示链接文字；光标移到链接上或紧挨着它，才显示完整的 `[文字](网址)`。导出的 PDF 中只显示文字。
- **打开链接**：按住 `⌘`（Windows 与 Linux 上为 `Ctrl`）点击链接，在浏览器中打开，正文与表格中都可以；按住时链接显示下划线。仅打开网页与邮件链接。

### 安装

- 已安装 v0.1.7 的，打开 Irori 后会收到更新提示，点「更新」即可，无需重新下载。
- **macOS**：`Irori_0.1.8_aarch64.dmg`（Apple Silicon）、`Irori_0.1.8_x64.dmg`（Intel）
- **Windows**：`Irori_0.1.8_x64-setup.exe` 或 `Irori_0.1.8_x64_en-US.msi`，任选其一
- **Linux**：`.deb`、`.rpm` 与 `.AppImage`

安装包尚未签名。macOS 若提示应用「已损坏」，可在终端执行
`xattr -dr com.apple.quarantine /Applications/Irori.app` 后再打开；
Windows 若出现 SmartScreen 提示，选择「更多信息 → 仍要运行」。

## v0.1.7 — 2026-09-19

v0.1.5 与 v0.1.6 没有单独发布，两者的内容一并收在这一版里。

### 更新

- **自动更新**：启动时检查 GitHub 上的新版本，有新版时在左下角提示；点「更新」后下载、校验签名并安装，随后自动重启，原先打开的文档会重新打开。
- 重启前，已有路径的文档就地保存；尚未保存过的文档会先请你保存，取消则不重启，更新留待下次。
- 抽屉的「设置」中显示当前版本，并可手动「检查更新」。
- 从这一版开始，之后的版本都可以在应用内直接更新；这一次仍需手动下载安装。

### 书写

- **列表**：`-`、`*`、`+` 与 `1.` 开头的列表项，符号着色，并按层级缩进；长项折行后与正文对齐，编号在点上右对齐（`9.` 与 `10.` 对齐）。
- **列表续写**：在列表项中按回车自动生成下一项，编号递增；在空项上按回车先退出一级，再结束列表。
- **移除**：段首连续输入两个空格自动转为全角缩进的功能已去除。

### 导出

- **PDF**：`⌘P` 或抽屉中的「导出为 PDF」，将全文导出为 A4 PDF。纸色、字体、字号与配色、标题与引用的样式、列表排布、行内的 Markdown 标记与图片，都与编辑器中所见一致；左侧目录、右侧页面预览与标题栏不在其中。段落只在行与行之间跨页，标题不会孤悬页底。
- macOS 上直接写入所选文件；Windows 与 Linux 上经由系统打印对话框，选择「另存为 PDF」即可。

### 修复

- `⌘N` 新开的窗口不再打开第一个窗口的文件，而是一张空白页。

### 安装

- **macOS**：`Irori_0.1.7_aarch64.dmg`（Apple Silicon）、`Irori_0.1.7_x64.dmg`（Intel）
- **Windows**：`Irori_0.1.7_x64-setup.exe` 或 `Irori_0.1.7_x64_en-US.msi`，任选其一
- **Linux**：`.deb`、`.rpm` 与 `.AppImage`

安装包尚未签名。macOS 若提示应用「已损坏」，可在终端执行
`xattr -dr com.apple.quarantine /Applications/Irori.app` 后再打开；
Windows 若出现 SmartScreen 提示，选择「更多信息 → 仍要运行」。

## v0.1.6 — 2026-09-18

### 导出

- **PDF**：`⌘P` 或抽屉中的「导出为 PDF」，将全文导出为 A4 PDF。纸色、字体、字号与配色、标题与引用的样式、列表排布、行内的 Markdown 标记与图片，都与编辑器中所见一致；左侧目录、右侧页面预览与标题栏不在其中。段落只在行与行之间跨页，标题不会孤悬页底。
- macOS 上直接写入所选文件；Windows 与 Linux 上经由系统打印对话框，选择「另存为 PDF」即可。

### 安装

- **macOS**：`Irori_0.1.6_aarch64.dmg`（Apple Silicon）、`Irori_0.1.6_x64.dmg`（Intel）
- **Windows**：`Irori_0.1.6_x64-setup.exe` 或 `Irori_0.1.6_x64_en-US.msi`，任选其一
- **Linux**：`.deb`、`.rpm` 与 `.AppImage`

安装包尚未签名。macOS 若提示应用「已损坏」，可在终端执行
`xattr -dr com.apple.quarantine /Applications/Irori.app` 后再打开；
Windows 若出现 SmartScreen 提示，选择「更多信息 → 仍要运行」。

## v0.1.5 — 2026-09-17

### 书写

- **列表**：`-`、`*`、`+` 与 `1.` 开头的列表项，符号着色，并按层级缩进；长项折行后与正文对齐，编号在点上右对齐（`9.` 与 `10.` 对齐）。
- **列表续写**：在列表项中按回车自动生成下一项，编号递增；在空项上按回车先退出一级，再结束列表。
- **移除**：段首连续输入两个空格自动转为全角缩进的功能已去除。

### 安装

- **macOS**：`Irori_0.1.5_aarch64.dmg`（Apple Silicon）、`Irori_0.1.5_x64.dmg`（Intel）
- **Windows**：`Irori_0.1.5_x64-setup.exe` 或 `Irori_0.1.5_x64_en-US.msi`，任选其一
- **Linux**：`.deb`、`.rpm` 与 `.AppImage`

安装包尚未签名。macOS 若提示应用「已损坏」，可在终端执行
`xattr -dr com.apple.quarantine /Applications/Irori.app` 后再打开；
Windows 若出现 SmartScreen 提示，选择「更多信息 → 仍要运行」。

## v0.1.4 — 2026-09-17

Irori 的首个公开版本。

### 书写

- **语法树式渲染**：标题、强调、引用、行内代码与链接依语法结构呈现样式，Markdown 标记保留在原文之中。
- **图片**：图片在行内直接显示，光标移入时还原为源码；粘贴的图片存放于文档旁的同名文件夹。
- **中文排版**：段首连续输入两个空格，自动转为全角缩进。
- **保存**：自动保存，间隔可调；文件在外部被修改时自动重新载入，若尚有未保存的改动，则由你选择保留哪一份。
- **查找**：`⌘F` 在全文中查找。

### 视图

- **页面预览**：右侧缩略图映射全文，可点击跳转，亦可拖动浏览。
- **目录**：左侧依一至三级标题生成，并标示当前所在的小节。
- **专注模式**：聚焦带之外的文字渐隐，聚焦范围与渐隐曲线均可调节，光标始终停留在聚焦带内。
- **标题栏**（macOS）：与纸面融为一体，文件名居中显示，整行均可拖动窗口；文件名前的圆点标示保存状态。

### 安装

- **macOS**：`Irori_0.1.4_aarch64.dmg`（Apple Silicon）、`Irori_0.1.4_x64.dmg`（Intel）
- **Windows**：`Irori_0.1.4_x64-setup.exe` 或 `Irori_0.1.4_x64_en-US.msi`，任选其一
- **Linux**：`.deb`、`.rpm` 与 `.AppImage`

安装包尚未签名。macOS 若提示应用「已损坏」，可在终端执行
`xattr -dr com.apple.quarantine /Applications/Irori.app` 后再打开；
Windows 若出现 SmartScreen 提示，选择「更多信息 → 仍要运行」。
